# 정담 앱 기능 목록

## 홈 탭

### 상단 헤더
- 앱 이름 "정담" 표시
- 사용자 이름 인사말
- FREE / PREMIUM 구독 배지

### 배너 캐러셀
- 3장 자동 슬라이드 (3.5초 간격)
- 하단 인디케이터 점

### 경조사 만들기
- 청첩장 만들기 카드
- 부고장 만들기 카드 (FREE 사용자 제한)

### 내가 주최한 경조사
- 진행중 / 최근 완료 탭
- 이벤트 카드: 날짜, 이름, 장소, 참여자 수, 총 부조금
- 페이지네이션 (5개씩, 점 스타일)

### 캘린더
- 월간 캘린더
- 이전/다음 월 이동
- 내 경조사 일정 점 표시

### 품앗이 장부
- 요약: 받은 금액 합계 / 낸 금액 합계
- 받음 탭: 게스트명, 관계, 금액 목록
- 줬음 탭: 받는사람, 금액, 행사명, 정산완료 토글
- 필터: 경조사별 / 관계(신랑측·신부측 등) / 세부관계(친구·직장 등)
- 페이지네이션 (5개씩, 점 스타일)
- 품앗이 기록 추가 버튼

### 감사 문자 (현재 미사용 - 제거 예정)
- 전체 / 미발송 필터
- 개별 메시지 편집 및 발송
- 모두 보내기

---

## 내 행사 탭

### 내가 주최한 경조사
- 모두 / 진행중 / 완료 필터
- 이벤트 카드: 날짜, 이름, 상태, 참여자·총부조금·평균 통계
- 상세보기 → EventDetailScreen
- 삭제 기능 (확인 모달)
- 미확정 내역 배너

### 참여한 경조사
- 참석한 행사 카드: 날짜, 이름, 메모
- 상세보기

---

## 이벤트 관련 화면

### 경조사 만들기 (CreateEventScreen)
- 2단계 위저드
- 청첩장 / 부고장 선택
- 이벤트 정보 입력 (이름, 날짜, 장소 등)
- 템플릿 선택

### 청첩장 미리보기 (EventDisplayScreen)
- 5가지 결혼식 템플릿: 클래식, 모던, 가든, 럭셔리, 빈티지
- 1가지 장례식 템플릿
- 이미지 슬라이드쇼
- QR코드 표시 (부조 수집용)
- 공유 기능

### 경조사 상세 관리 (EventDetailScreen)
- 게스트 목록 (전체 / 확인됨 / 미확인 탭)
- 정렬: 기본 / 금액높음 / 금액낮음 / 이름순
- 게스트 검색
- 각 게스트 확인 토글
- 통계 모달: 총액, 평균, 확인율
- 페이지네이션 (10개씩)

### 태블릿 하객 접수 패드 (GuestWriting → GuestConfirmScreen)

**레이아웃 — 가로 2열 고정**
- 좌측 (38%): 서명 이미지 + 다시쓰기 버튼
- 우측 (59%): 이름 / 축의금 / 관계 / 영수증 — 한 화면에 스크롤

---

**좌측 패널**
- 상단: 신랑측 / 신부측 뱃지 (색상: 파랑 / 핑크)
- 중앙: 필기 이미지 카드 (테두리 박스, 꽉 채움)
- 하단: "↩ 다시 쓰기" 버튼
- 우상단: "✕ 나가기" 버튼 (고정)

**우측 패널 — 섹션별 한 스크롤**

섹션 1. 성함
- OCR 인식된 이름 후보 칩 (최대 5개) — 탭하면 입력창 자동 반영
- 이름 직접 입력창 (인식 결과 없으면 자동 포커스)
- 입력창 우측 X 버튼으로 초기화

섹션 2. 축의금
- 증액 칩 4개: +1만 / +3만 / +5만 / +10만 (탭할 때마다 누적)
- 선택 금액 확인 바: "선택 금액 — [금액]" + 초기화 버튼
- 빠른 선택 그리드: 3만 / 5만 / 7만 / 10만 / 15만 / 20만 + 직접입력
- 직접입력 탭 시 모달: 금액 크게 표시 + 증액 칩 + 직접 숫자 입력

섹션 3. 관계
- 4개 버튼: 친척 / 친구 / 직장 / 기타 (토글 선택)

섹션 4. 카카오톡 영수증 (선택)
- "📱 카카오톡으로 영수증 받기" 토글 버튼 — 기본 접힘
- 탭하면 전화번호 입력창 열림 (자동 포커스 + 스크롤 이동)
- 010-xxxx-xxxx 자동 포맷
- 번호 입력 후 기록 시 카카오 알림톡으로 영수증 자동 발송
- 번호 없어도 기록 가능

**액션 버튼**
- "기록하기" — 메인 CTA, 이름 입력 시 활성화
- "금액 없이 이름만 기록할게요" — 테두리 버튼 (이름만 저장)

**완료 화면**
- 감사 애니메이션 2.5초 (🙏 + 이름 + 방명록 기록 완료 텍스트)
- 자동으로 서명 화면으로 전환

**기타**
- 8인치~12인치 태블릿 반응형 (SH 기준 스케일)
- 가로 모드 전용

---

## 가이드 탭

### 역할 선택
- 행사를 준비해요 (주최자)
- 행사에 참여해요 (참석자)

### 주최자 가이드
- 결혼식 준비 가이드
- 장례식 준비 가이드
- 예산 계산기 (준비중)
- 업체 리스트 (준비중)
- 자주묻는 질문 (FAQ)
- 인기 가이드 3개
- 랜덤 꿀팁
- 체크리스트 퀴즈

### 참석자 가이드
- 축의금·조의금 AI 추천
- 복장 가이드
- 예절·매너 가이드
- 추천 서비스 (준비중)
- 인기 가이드 3개
- 랜덤 꿀팁
- 경조사 매너 퀴즈

---

## 프로필 탭

### 사용자 정보
- 이름, 전화번호 (숨김)
- 가입일
- 정보 편집 (미구현)

### 나의 활동 통계
- 총 경조사 수
- 총 부조금 합계
- 총 참석자 수

### 관리 메뉴
- 내 경조사 이동
- 부조 기록 (준비중)
- 알림 설정 (준비중)
- 통계 보기 (준비중)

### 도구 메뉴
- QR 코드 생성 (준비중)
- 데이터 내보내기 Excel/PDF (준비중)
- 백업 및 복원 (준비중)

### 기타 메뉴
- 앱 공유하기
- 도움말 (준비중)
- 약관 및 개인정보처리방침 (준비중)
- 문의하기 (준비중)

### 계정
- 로그아웃
- 계정 삭제 (준비중)
