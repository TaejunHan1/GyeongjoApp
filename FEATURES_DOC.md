# 경조사 앱 기능 명세서
## 대상 화면: EventDetailScreen (부조금 상세보기) + 디지털 방명록 전체 흐름

---

## 1. EventDetailScreen — 부조금 상세보기

### 진입 방법
- MyEventsScreen → 주최한 경조사 탭 → 행사 카드의 **부조금 상세보기** 버튼 탭

---

### 상단 헤더
| 항목 | 내용 |
|---|---|
| 행사 이미지 아이콘 | 예식 봉투 이미지 (Wedding_Cash_Gifts_List.png) |
| 행사 이름 | 이벤트명 텍스트 |
| 행사 날짜 | 연·월·일·요일 포맷 (예: 2024년 6월 15일 (토)) |
| 장소 | 장소명 또는 "장소 미정" |

---

### 총 부조금 요약 카드
| 항목 | 내용 |
|---|---|
| 총 부조금 | 전체 금액 합계 (크게 표시) |
| 총 건수 | 등록된 부조 내역 수 |
| 평균 금액 | 총액 ÷ 건수 |

---

### 액션 버튼 4개 (가로 그리드)

| 버튼 | 아이콘 이미지 | 동작 |
|---|---|---|
| 부조 추가 | Additional_Condolence_Donations.png | 부조 추가 모달 오픈 |
| 통계 | Funeral_Donation_Records.png | 통계 모달 오픈 |
| 확정관리 | check.png | 확정 관리 모달 오픈 |
| 디지털 방명록 | 연필 아이콘 (Ionicons pencil) | 방명록 접수대 선택 모달 오픈 |

---

### 디지털 방명록 현황 카드
- **조건**: `input_method === 'handwriting'` 인 항목이 1건 이상일 때만 자동 노출
- **표시 항목**:
  - 총 방문 인원 (handwriting 항목 전체 수)
  - 금액 입력 건수 (amount가 있는 handwriting 항목 수)
  - 현장 누적 금액 (handwriting 항목 amount 합계)
  - 최근 등록 3명 미리보기: 이름 + 금액 (또는 "미입력")

---

### 부조 내역 리스트

#### 탭 필터 (상단 3개 탭)
| 탭 | 표시 조건 | 괄호 내 숫자 |
|---|---|---|
| 전체 | 모든 항목 | contributions.length |
| 미확정 | is_verified === false | 미확정 항목 수 |
| 확정 | is_verified === true | 확정 항목 수 |

#### 정렬 버튼 (4개 가로 배열)
| 버튼 | 정렬 기준 | 기본값 |
|---|---|---|
| 등록순 | created_at 내림차순 (최신이 맨 위) | ✓ 기본값 |
| 금액 많은순 | amount 내림차순 | |
| 금액 적은순 | amount 오름차순 | |
| 이름순 | guest_name 가나다 오름차순 | |

> 탭·정렬·검색어 변경 시 자동으로 1페이지로 이동

#### 검색바
- 탭하면 **이름 검색 모달** 오픈 (전체화면 슬라이드 모달)
- 검색어 적용 후 바에 검색어 표시 + 우측 ✕ 버튼으로 초기화

#### 내역 아이템 표시 항목
| 항목 | 내용 |
|---|---|
| 아바타 | 이름 첫 글자 원형 아이콘 |
| 이름 | guest_name |
| 관계 | `신랑측 · 친구` 형식 (relation_category · relation_detail) |
| 금액 | formatAmount 함수 적용 (만원 단위 표시) |
| 날짜 | created_at → M월 D일 형식 |
| 확정 상태 시각 구분 | 확정: 파란 좌측 테두리 스타일 / 미확정: 회색 테두리 |

#### 내역 아이템 액션 버튼
**미확정 항목:**
- **확정** — 확정 확인 바텀시트 오픈
- **수정** — 부조 수정 모달 오픈
- **삭제** — 삭제 확인 Alert → 삭제 처리

**확정된 항목:**
- **확정취소** — 확정 확인 바텀시트 오픈 (취소 방향)

> 단, `id`가 `groom-N` / `bride-N` 형식인 샘플 데이터는 액션 버튼 미표시

#### 페이지네이션
- 10건씩 페이지 분할
- 총 페이지 2 이상일 때만 표시
- 이전(◀) / 다음(▶) 버튼 + `현재 / 전체` 페이지 텍스트

#### 빈 상태
- 검색 결과 없음: "검색 결과가 없어요 · 다른 이름으로 검색해보세요"
- 부조 내역 없음: "부조 내역이 없어요 · 첫 번째 부조를 추가해보세요"

---

### 실시간 업데이트 방식 (이중 구독)
1. **DeviceEventEmitter** (`guestbook_new_entry` 이벤트) — 같은 기기에서 등록 즉시 반영
2. **Supabase Realtime** (`postgres_changes`) — 다른 기기에서 등록해도 반영
   - INSERT → 목록 맨 위 추가 (중복 방지: `prev.some(c => c.id === entry.id)`)
   - UPDATE → 해당 항목 자리에서 즉시 변경
   - DELETE → 해당 항목 즉시 제거
   - 서버 필터 없음 (client-side에서 `entry.event_id !== eventId`로 필터링) — RLS 미설정 환경에서도 동작

---

---

## 2. EventDetailScreen 내 모달 전체

---

### ① 부조 추가 모달
- **진입**: 액션 버튼 "부조 추가" 탭
- **형식**: 전체화면 pageSheet 슬라이드
- **헤더**: ← 뒤로 버튼 / "부조 추가" 제목 / "완료" 저장 버튼

| 입력 항목 | 형식 | 비고 |
|---|---|---|
| 성함 | 텍스트 입력 | 필수 |
| 부조금 | 숫자 입력 (자동 콤마 포맷) | 1,000원 이상 필수 |
| 측 선택 | 신랑측 / 신부측 버튼 | 기본값: 신랑측 |
| 관계 선택 | 친구 / 동료 / 가족 / 친척 / 기타 버튼 | 기본값: 친구 |

- 저장 성공 → Alert 팝업 → 확인 탭 → 모달 닫힘 + 데이터 새로고침

---

### ② 부조 수정 모달
- **진입**: 미확정 내역 아이템의 "수정" 버튼 탭
- **형식**: 전체화면 pageSheet 슬라이드
- **헤더**: ← 뒤로 버튼 / "부조 수정" 제목 / "완료" 저장 버튼

| 입력 항목 | 형식 | 비고 |
|---|---|---|
| 성함 | 기존 값 미리 채워짐 | 필수 |
| 부조금 | 기존 값 미리 채워짐 (자동 포맷) | 1,000원 이상 필수 |
| 측 선택 | 기존 relation_category 반영 | 신랑측 / 신부측 |
| 관계 선택 | 기존 relation_detail 반영 | 친구 / 동료 / 가족 / 친척 / 기타 |

- **제한**: 확정된 항목(is_verified = true)은 수정 버튼 자체가 미표시

---

### ③ 통계 모달
- **진입**: 액션 버튼 "통계" 탭
- **형식**: 전체화면 pageSheet 슬라이드 + ScrollView

**총 요약 카드**
| 항목 | 값 |
|---|---|
| 총 부조금 | contributions 전체 amount 합계 |
| 총 건수 | contributions.length 건 |
| 평균 부조금 | 총액 ÷ 건수 |

**금액대별 분포 바차트**
| 구간 | 조건 |
|---|---|
| ~5만 | amount < 50,000 |
| 5~10만 | 50,000 ≤ amount < 100,000 |
| 10~20만 | 100,000 ≤ amount < 200,000 |
| 20만~ | amount ≥ 200,000 |
- 각 구간별 건수 표시
- 최대값 기준 100% 높이의 바 차트 렌더링

**확정 현황 프로그레스바**
- 가로 프로그레스바: 확정 비율 (파란색 채움)
- 확정: N건 (파란 점)
- 미확정: N건 (회색 점)

**관계별 통계 리스트**
- relation_detail 기준 그룹핑
- 각 관계: 건수 + 금액 합계
- 금액 많은 순으로 정렬

---

### ④ 확정 관리 모달
- **진입**: 액션 버튼 "확정관리" 탭
- **형식**: 전체화면 pageSheet 슬라이드 + ScrollView

**상단 요약 카드 (2분할)**
| 섹션 | 표시 내용 |
|---|---|
| 확정됨 (초록 아이콘) | 확정 건수 + 확정 금액 합계 |
| 미확정 (주황 아이콘) | 미확정 건수 + 미확정 금액 합계 |

**미확정 부조 목록**
- 조건: 미확정 항목이 1건 이상일 때 노출
- 헤더 우측에 **전체 확정** 버튼 (탭 → Alert 확인 → 전체 일괄 확정 처리)
- 각 항목: 이름 / 관계·금액 / 개별 **확정** 버튼

**확정된 부조 목록**
- 조건: 확정 항목이 1건 이상일 때 노출
- 각 항목: ✓ 아이콘 + 이름 / 관계·금액 / **취소** 버튼 (확정 해제)

---

### ⑤ 확정 확인 바텀시트
- **진입**: 내역 아이템의 "확정" 또는 "확정취소" 버튼 탭
- **형식**: 투명 오버레이 + 하단에서 슬라이드 올라오는 바텀시트
- **표시 항목**:
  - 핸들바
  - 이름 (`{이름}님의 부조` 제목)
  - 금액 (크게 표시)
  - 관계 (신랑측 · 친구 형식)
  - 설명 문구
    - 확정 전: "확정하면 내역을 더 이상 수정하거나 삭제할 수 없습니다."
    - 확정 후: "확정을 취소하면 내역을 다시 수정하거나 삭제할 수 있습니다."
- **버튼**:
  - 미확정 → "확정하기" (파란색)
  - 확정됨 → "확정 취소" (경고색)
  - 하단 "취소" 버튼 (바텀시트 닫기)
- 배경 탭 시 닫힘

---

### ⑥ 이름 검색 모달
- **진입**: 검색바 탭
- **형식**: 투명 오버레이 + 하단에서 슬라이드 올라오는 전체화면 모달
- **헤더**: ← 뒤로 버튼 / "부조 검색" 제목 / "완료" 버튼
- **검색 입력창**: 자동 키보드 활성화 (editable 토글 트릭 적용)
- **검색 결과 미리보기**: 검색어 입력 시 최대 5건 실시간 표시
  - 각 항목: 이름 / 관계 / 금액
  - 결과 없음: "검색 결과가 없어요" 안내
  - 검색어 없음: "검색어를 입력해주세요" 빈 상태
- **완료** 탭 → 검색어 적용 + 모달 닫힘
- ← 버튼 탭 → 검색어 변경 취소 + 모달 닫힘

---

### ⑦ 디지털 방명록 접수대 선택 모달
- **진입**: 액션 버튼 "디지털 방명록" 탭
- **형식**: 투명 오버레이 + 중앙 다이얼로그 (fade 애니메이션)
- **상단**: 📋 아이콘 + "방명록 접수대 선택" 제목 + "담당자가 한 번만 선택하면 됩니다." 부제

**카드 2개 가로 배치**
| 카드 | 색상 | 내용 | 동작 |
|---|---|---|---|
| 신랑측 🤵 | 파란색 계열 | 신랑 가족·친구·동료 하객 | GuestWritingScreen (side: 'groom') |
| 신부측 👰 | 핑크색 계열 | 신부 가족·친구·동료 하객 | GuestWritingScreen (side: 'bride') |

- 하단 **취소** 버튼 → 모달 닫기

---

---

## 3. 디지털 방명록 (GuestWritingScreen + GuestConfirmScreen)

### 전체 흐름

```
EventDetailScreen
  └─ [디지털 방명록] 버튼 탭
       └─ 접수대 선택 모달 (신랑측 / 신부측 카드 선택)
            └─ GuestWritingScreen (intro 모드 — 세로)
                 └─ [방명록 시작하기] 탭
                      └─ GuestWritingScreen (drawing 모드 — 전체화면 가로)
                           └─ 하객이 터치펜으로 성함 필기
                                └─ [완료 ✓] 탭
                                     └─ GuestConfirmScreen (가로 유지)
                                          ├─ [단계 1] 이름 확인
                                          │    ├─ ML Kit 필기 인식 (EAS 빌드만 동작)
                                          │    ├─ 후보 탭으로 선택 또는 직접 입력
                                          │    └─ [네, 맞습니다 ✓] 탭 → 단계 2
                                          └─ [단계 2] 금액·관계 입력
                                               ├─ 증액 칩 (+1만 / +3만 / +5만 / +10만)
                                               ├─ 선택 금액 확인 바
                                               ├─ 빠른 선택 프리셋 (3만~20만 또는 행사 설정값)
                                               ├─ 직접입력 버튼 → 직접입력 모달
                                               ├─ 관계 선택 (친척 / 친구 / 직장 / 기타)
                                               ├─ [기록하기] 탭 → guest_book INSERT
                                               │    └─ "감사합니다" 화면 2.5초 → intro 복귀
                                               └─ [금액 없이 이름만 기록할게요] 탭 → amount=null INSERT
```

---

## 4. GuestWritingScreen 상세 기능

### 화면 진입 파라미터
- `event`: 행사 객체 (event_type, groom_name, bride_name, event_name 등)
- `side`: `'groom'` 또는 `'bride'` (접수대 선택 모달에서 결정됨)

---

### Intro 모드 (세로 방향)

| 항목 | 내용 |
|---|---|
| 상태 | `mode === 'intro'` — 자동으로 세로(PORTRAIT_UP) 잠금 |
| ✕ 뒤로 버튼 | 좌상단 원형 버튼 → navigation.goBack() |
| 측 배지 | 신랑측(🤵 파랑) / 신부측(👰 핑크) — 선택한 측 색상으로 표시 |
| 행사명 | 결혼식: `신랑이름 ♥ 신부이름` / 기타: event_name |
| 부제목 | "결혼식에 오신 것을 환영합니다" |
| 안내 박스 | 📱 아이콘 + "시작 버튼을 누르면 화면이 가로 방향 전체화면으로 전환됩니다. 하객분이 터치펜으로 성함만 적으시면 됩니다." |
| 방명록 시작하기 버튼 | 측 색상 배경 → 탭 시 LANDSCAPE 전환 + drawing 모드 진입 |

---

### Drawing 모드 (가로 전체화면)

| 항목 | 내용 |
|---|---|
| 상태 | `mode === 'drawing'` — LANDSCAPE 잠금 |
| 배경 | 베이지 종이 느낌 (#FDFAF5) |
| 가이드라인 | SVG로 60px 간격 수평 연한 선 (종이 질감) |
| 필기 렌더링 | SVG Path + strokeWidth 5, strokeLinecap round |
| 빈 상태 안내 | 중앙에 "성함을 적어주세요" 연한 힌트 텍스트 (터치 불감) |
| 화면 켜짐 유지 | expo-keep-awake 적용 |

**플로팅 컨트롤 버튼 (Animated.View — 자동 반투명)**
- 3초 무입력 → opacity 0.15로 서서히 페이드
- 터치 시 → opacity 1.0으로 즉시 복귀

| 버튼 | 위치 | 동작 |
|---|---|---|
| ✕ 나가기 | 좌상단 | 캔버스 초기화 + intro 모드 복귀 + 세로 전환 |
| ↺ 다시 쓰기 | 우하단 | 캔버스 초기화 (strokes, currentPath 초기화) |
| 완료 ✓ | 우하단 (측 색상) | ViewShot으로 캔버스 PNG 캡처 → GuestConfirmScreen 이동 |

> 완료 버튼은 필기가 없으면(hasStrokes = false) 비활성화 (opacity 28%)

**10초 무입력 자동 초기화**
- 마지막 터치 후 10초 경과 시 캔버스 자동 초기화 (clearCanvas)

---

### 팜 리젝션 (Palm Rejection) 알고리즘

**목표**: 태블릿에서 손바닥이 화면에 닿아도 펜 필기가 정확하게 인식됨

**구현 방법** (PanResponder 기반):

| 케이스 | 처리 방법 |
|---|---|
| iOS Apple Pencil | `touchType === 'stylus'` 감지 → 해당 터치 즉시 펜으로 확정 |
| 이미 그리는 중 새 터치 | drawingTouchId가 이미 있으면 새 터치는 무시 (손바닥 판단) |
| 터치 1개만 있을 때 | 즉시 그리기 시작 (지연 없음) |
| 터치 여러 개 동시 감지 | 모두 `pendingTouches` Map에 등록 후 대기 |

**펜 확정 기준** (onPanResponderMove에서):
- `PEN_MOVE_THRESHOLD = 6px`
- 각 후보 터치가 시작 위치에서 얼마나 이동했는지 계산 (Math.hypot)
- 6px 이상 이동한 터치 중 가장 많이 이동한 것을 펜으로 확정
- 손바닥: 거의 이동 없음 → 펜: 글자 쓰기로 빠르게 이동

**Release 처리**:
- 확정된 펜 터치가 떼지면 → 현재 획을 strokes 배열에 저장
- 손바닥이 먼저 떼지면 → pendingTouches에서만 제거, 그리기 계속

---

### 화면 방향 관리 (expo-screen-orientation)

| 상황 | 방향 |
|---|---|
| intro 모드 진입 시 (useEffect) | PORTRAIT_UP 잠금 |
| GuestConfirm에서 돌아올 때 | intro mode useEffect가 자동 PORTRAIT_UP 처리 |
| "방명록 시작하기" 탭 시 | LANDSCAPE 잠금 |
| "✕ 나가기" 탭 시 | PORTRAIT_UP 잠금 + intro 모드 전환 |
| 화면 언마운트 시 | PORTRAIT_UP 잠금 (cleanup) |

> 완료 버튼 → GuestConfirmScreen 이동 시에는 방향 변경 없음 (가로 유지)

---

---

## 5. GuestConfirmScreen 상세 기능

### 화면 진입 파라미터
- `event`: 행사 객체
- `handwritingUri`: ViewShot으로 캡처한 PNG 파일 경로 (tmpfile)
- `side`: `'groom'` 또는 `'bride'`

### 레이아웃
- **가로 2열 구성** (isLandscape: width > height일 때)
  - 좌측 패널: 전체 너비의 42% — 필기 이미지 표시
  - 세로 구분선
  - 우측 패널: 전체 너비의 55% — 이름·금액 입력

---

### 로딩 화면
- 화면 진입 즉시 ML Kit 필기 인식 시작
- 인식 중: `ActivityIndicator` + "필기 내용 인식 중..." 텍스트 표시

---

### 좌측 패널 — 필기 이미지

| 항목 | 내용 |
|---|---|
| 라벨 | "✍️ 작성하신 내용" |
| 이미지 영역 | 흰색 종이 카드 스타일 (borderRadius 14, 그림자 효과) |
| 필기 이미지 | handwritingUri PNG를 contain 모드로 표시 |
| 다시 쓰기 버튼 | "↩ 다시 쓰기" — 저장 없이 navigation.goBack() → GuestWritingScreen 복귀 |

---

### 우측 패널 — 단계 1: 이름 확인

**진입 조건**: `nameConfirmed === false`

| 항목 | 내용 |
|---|---|
| 아이콘 | 👤 측 색상 배경 아이콘 박스 |
| 제목 | "이름 확인" |
| 부제목 | 후보 있음: "인식된 이름을 선택하거나 직접 수정하세요" / 없음: "아래에 직접 이름을 입력해주세요" |

**ML Kit 인식 결과 (EAS 빌드에서만 동작)**
- 인식 결과를 후보 버튼으로 최대 3개 표시
- 첫 번째 후보가 기본 선택값으로 설정
- 탭 시 해당 이름이 입력창에 채워짐
- 선택된 후보: 측 색상 테두리 + 배경 강조

**Expo Go 환경 (ML Kit 미지원)**
- 후보 버튼 없음 (TextRecognition require 실패 시 null 처리)
- 입력창만 표시

**직접 입력창**
- ✏️ 아이콘 + TextInput
- border 색상: 입력값 있으면 측 색상 / 없으면 회색
- 최대 20자

**하단 확인 버튼**
- "네, 맞습니다 ✓" — 입력값 있을 때 활성화 (측 색상 배경)
- 비어있으면 opacity 35% 비활성화
- 탭 시 → `nameConfirmed = true` → 단계 2로 전환

---

### 우측 패널 — 단계 2: 금액·관계 입력

**진입 조건**: `nameConfirmed === true`

**인사 문구**
- `{이름} 님, 축의금을 기록할게요 👋`

---

**증액 칩 (4개 가로 배열)**
| 칩 | 금액 | 동작 |
|---|---|---|
| +1만 | +10,000원 | 현재 금액에 누적 합산 |
| +3만 | +30,000원 | 현재 금액에 누적 합산 |
| +5만 | +50,000원 | 현재 금액에 누적 합산 |
| +10만 | +100,000원 | 현재 금액에 누적 합산 |

> 탭할수록 금액이 올라감 (예: +3만 두 번 → 6만원)

---

**선택 금액 확인 바** (항상 노출)
| 상태 | 표시 |
|---|---|
| 금액 미선택 | "선택 금액" + "—" (회색) |
| 금액 선택 | "선택 금액" + 해당 금액 (측 색상 강조) + "초기화" 버튼 |

---

**빠른 선택 프리셋 버튼 그리드**
- 행사 `preset_amounts` 설정값이 있으면 그 값 사용
- 없으면 기본값: 3만 / 5만 / 7만 / 10만 / 15만 / 20만
- 마지막에 **직접입력** 버튼 항상 포함
- 탭 시 해당 금액으로 SET (증액 칩과 달리 교체 방식)
- 선택된 금액: 측 색상 테두리 + 배경 강조

---

**관계 선택 (4개 가로 배열)**
| 버튼 | 선택 시 |
|---|---|
| 친척 | 측 색상 테두리 + 배경 |
| 친구 | 측 색상 테두리 + 배경 |
| 직장 | 측 색상 테두리 + 배경 |
| 기타 | 측 색상 테두리 + 배경 |

- 재탭 시 선택 해제 (토글)
- 미선택 시 저장 시 자동으로 '기타' 적용

---

**하단 버튼 2개**
| 버튼 | 조건 | 동작 |
|---|---|---|
| 기록하기 | 금액 선택 시 활성화 (측 색상) | guest_book INSERT + 감사 화면 |
| 금액 없이 이름만 기록할게요 | 항상 표시 (회색 텍스트) | amount=null로 INSERT + 감사 화면 |

---

### 직접입력 모달 (금액)

- **진입**: "직접입력" 프리셋 버튼 탭
- **형식**: 중앙 다이얼로그 (fade 애니메이션)

| 항목 | 내용 |
|---|---|
| 금액 표시 | 입력 중인 숫자를 크게 표시 (44px) + "원" 단위 |
| 만원 변환 힌트 | 입력값을 만원 단위로 변환해서 보여줌 (예: 50000 → 5만원) |
| 증액 칩 (4개) | +1만 / +5만 / +10만 / +20만 — 현재 값에 누적 합산 |
| 직접 입력창 | 숫자 키패드, "직접 입력" 라벨, "원" 단위 표시 |
| 버튼 3개 | 취소 (모달 닫기) / 초기화 (입력값 지우기) / 확인 (값 적용) |

- 1,000원 미만 입력 시 Alert "1,000원 이상 입력해주세요"
- 입력값을 10,000원 단위로 반올림 적용 (10,000원 미만이면 그대로)

---

### 저장 처리 (handleSave)

**순서:**
1. 필기 이미지를 Supabase Storage (`event-images/handwriting/{event.id}/{timestamp}.png`)에 업로드
   - 업로드 실패해도 계속 진행 (handwritingImageUrl = null 처리)
2. `guest_book` 테이블에 INSERT

**저장 데이터 (guest_book 테이블)**
| 컬럼 | 값 |
|---|---|
| event_id | 행사 ID |
| guest_name | 확인된 성함 (trim 처리) |
| guest_phone | null |
| amount | 선택 금액 (이름만 기록 시 null) |
| relation_category | `side === 'groom' ? '신랑측' : '신부측'` |
| relation_detail | 선택한 관계 또는 '기타' (미선택 시) |
| is_verified | false (기본값) |
| input_method | 'handwriting' |
| handwriting_image_url | Supabase Storage URL (업로드 실패 시 null) |
| side | 'groom' 또는 'bride' |

3. DeviceEventEmitter로 `guestbook_new_entry` 이벤트 발행 → EventDetailScreen 즉시 반영
   - INSERT 실패 시에도 로컬 fallback 데이터 발행 (id: `local_{timestamp}`)

---

### 감사 화면

| 항목 | 내용 |
|---|---|
| 조건 | `saved === true` |
| 이모지 | 🙏 (80px) |
| 제목 | "감사합니다" |
| 이름 | "{이름} 님" |
| 부제목 | "방명록이 기록되었습니다" |
| 하단 강조선 | 측 색상 가로 선 |
| 자동 이동 | 2.5초 후 GuestWritingScreen (intro 모드) 자동 복귀 |

> 복귀 후 GuestWritingScreen의 `mode === 'intro'` useEffect가 자동으로 세로(PORTRAIT_UP) 전환 처리

---

---

## 6. 데이터 흐름 요약

```
하객 필기 완료
  → ViewShot PNG 캡처 (tmpfile)
  → GuestConfirmScreen: ML Kit 인식 (EAS) 또는 수동 입력
  → 이름 확인 → 금액·관계 선택
  → Supabase Storage에 PNG 업로드
  → guest_book 테이블 INSERT
  → DeviceEventEmitter emit (같은 기기 즉시 반영)
  → Supabase Realtime 수신 (다른 기기에도 반영)
  → EventDetailScreen 부조 내역 리스트 맨 위에 즉시 추가
  → 감사 화면 2.5초
  → GuestWritingScreen intro 복귀 → 다음 하객 대기
```

---

## 7. 사후 관리

- 방명록 등록 내역은 `is_verified = false`로 저장됨
- 행사 후 확정관리 모달에서 일괄 확정 처리 가능
- 필기 이미지는 Supabase Storage에 보관 → 나중에 확인 가능
- 통계 모달에서 현장 등록분 포함한 전체 현황 확인 가능
- 검색 모달에서 이름으로 특정 하객 내역 찾기 가능

---

## 8. 기술 주의사항

| 항목 | 내용 |
|---|---|
| ML Kit 텍스트 인식 | Expo Go 미지원 → EAS dev client 빌드 필요 |
| 팜 리젝션 | Apple Pencil(iOS) 완벽 지원 / 일반 스타일러스는 6px 이동 임계값 소프트웨어 처리 |
| 화면 방향 | 필기 중 가로 고정, 완료/나가기 시 세로 복귀 (세로 복귀는 GuestWritingScreen intro useEffect 담당) |
| 오프라인 대응 | 이미지 업로드 실패해도 이름/금액/관계는 별도 저장 |
| 실시간 구독 | Supabase filter 없음 → client-side event_id 필터링 (RLS 미설정 환경 대응) |
| 중복 방지 | `prev.some(c => c.id === entry.id)` 체크로 DeviceEventEmitter + Supabase Realtime 중복 추가 방지 |
